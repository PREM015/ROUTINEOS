# Project Instructions

Project path:

`D:\code\projects\daily-plan`

## Primary Reference

Always read and use:

`D:\code\projects\daily-plan\PLAN.MD`

The `PLAN.MD` file is the primary source of truth for the project's planned implementation.

## Main Objective

The project was previously generated by another website up to Phase 31. However, during generation, some phases did not create all the files mentioned in `PLAN.MD`, and many required files were missed or incompletely implemented.

Your task is to bring the project to a fully completed state through **Phase 31**.

## Required Work

Read `PLAN.MD` carefully and completely.

Go through **every phase from Phase 0 through Phase 31** and compare the files and implementation described in each phase with the current project.

For every phase:

* Check which required files already exist.
* Check which required files are missing.
* Check which existing files are incomplete.
* Check which files contain placeholders, TODOs, stubs, or unfinished implementations.
* Check whether the implementation actually matches the requirements of that phase.
* Create every missing file required by the plan.
* Properly implement every incomplete or missing file according to `PLAN.MD`.
* Make sure dependencies between phases and files are correctly handled.

Do not assume that a file is complete merely because it exists.

## Important

Do **not** stop after entering or completing one phase.

Do **not** ask the user to say "next".

Work continuously through **Phase 0 → Phase 31** and complete all required work.

The goal is that every file mentioned in `PLAN.MD` for Phase 0–31 exists and is properly implemented according to the plan.

Preserve existing correct implementations. Do not unnecessarily rewrite working code.

After implementation, verify the project and fix relevant errors caused by the work.

Finally, re-check the project against `PLAN.MD` to ensure that no required files or implementations from Phase 0–31 were missed.
